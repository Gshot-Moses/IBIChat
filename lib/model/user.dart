

class User {
  int id;
  String identifier;
  String first_name;
  String last_name;
  String title;
  String phone;

  User({required this.id, required this.identifier, required this.first_name,
    required this.last_name,
    required this.title,
    required this.phone});
}
